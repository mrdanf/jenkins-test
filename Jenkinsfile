pipeline {
    agent any

    environment {
        // Umgebungsvariablen, falls nötig
    }

    stages {
        stage ('End - Echo') {
            steps {
                sh 'Hello, END!'
            }
        }
    }
}
