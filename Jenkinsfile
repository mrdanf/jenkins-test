pipeline {
    agent any

    stages {
        stage ('End - Echo') {
            steps {
                sh 'echo "Hello, END!"'
                sh 'echo "pwd: $(pwd)"'
            }
        }
    }
}
